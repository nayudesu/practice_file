$('.comment-textarea').val("")
$('.book-comments-counter').html("<%= j(render 'comments/count', book: @comment.book) %>")
$('.book-comments-index').html("<%= j(render 'comments/comment', book: @comment.book) %>")