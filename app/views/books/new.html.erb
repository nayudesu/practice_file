<h1>New Book</h1>
<%= form_with model: @book do |f| %>
<label for='title'>Title</label> <!--紐つける-->
  <%= f.text_field :title, autofocus: true, id:"Title" %>
<label>Opinion</label>
  <%= f.text_field :body %>
  
<%= f.submit %>
<% end %> 
